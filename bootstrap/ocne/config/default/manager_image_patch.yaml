apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: ghcr.io/verrazzano/cluster-api-ocne-bootstrap-controller-amd64
          name: manager
