apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: iad.ocir.io/odsbuilddev/aamitra/dev/ocne-bootstrap-controller-amd64
          name: manager
